[package]
name = "lfff-cli"
version = "2.0.4"
edition = "2024"
description = "CLI frontend for LibreFastbootFirmwareFlasher"
license = "GPL-3.0"

[[bin]]
name = "lfff"
path = "src/main.rs"

[dependencies]
lfff-lib = { path = "../lib" }
clap = { version = "4", features = ["derive"] }
log = "0.4"
env_logger = "0.11"
colored = "3"
zip = { version = "2", default-features = false, features = ["deflate"] }

[dev-dependencies]
assert_cmd = "2"
